'use strict';

describe('ChartActionCreators', () => {
  let action;

  beforeEach(() => {
    action = require('actions/ChartActionCreators.js');
  });

  it('should be defined', () => {
    expect(action).toBeDefined();
  });
});
